import { Meta, Story, Canvas, Stories, ArgsTable } from '@storybook/addon-docs';

import App from '@/views/app.vue'

export const Template = (args) => ({
    components: {App  },
    setup() {
      return { args };
    },
    template: '<app />'
});

<Meta title="Layouts/Login" decorators={[() => ({ template: '<div id="app" class="fixed h-screen w-screen top-0 left-0"><story /></div>' })]} />

<Story name="Default">
  {Template.bind({})}
</Story>