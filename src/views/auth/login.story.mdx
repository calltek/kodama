import { Meta, Story, Canvas, Stories, ArgsTable } from '@storybook/addon-docs';

import App from '../app.vue'

export const Template = (args) => ({
    components: {App  },
    setup() {
      return { args };
    },
    template: '<app />'
});

<Meta title="Layouts/Login" decorators={[() => ({ template: '<div id="app"><story /></div>' })]} />

<Story name="Default">
  {Template.bind({})}
</Story>