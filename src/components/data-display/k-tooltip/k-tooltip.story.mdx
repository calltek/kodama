import { Meta, Story, Canvas, Stories, ArgsTable } from '@storybook/addon-docs';
import { ref } from 'vue';
import { renderParameters } from '../../../helpers/storybook'
import SHead from '../../../../.storybook/app/components/s-head.jsx'

export const Template = (args) => ({
    setup() {
      return { args };
    },
    template: '<k-tooltip v-bind="args"><k-button color="primary">Tooltip</k-button></k-tooltip>'
});

<Meta title="Plugins/Tooltip" decorators={[() => ({ template: '<div class="flex items-center justify-center p-10"><story /></div>' })]} />

<SHead image="https://media0.giphy.com/media/dUJxUPSkmZBqE/giphy.gif?cid=ecf05e47bc59lioy7qbcd3hxymndanryn3rqx3dupt1obgzr&rid=giphy.gif&ct=g">💭 KTooltip</SHead>

# KTooltip
Con el componente `k-tooltip` puedes mostrar información adicional sobre un elemento al pasar por encima o clicar sobre el.

<Story name="Default" parameters={renderParameters({showPanel: true})} args={{content:'Lorem ipsum dolor sit', placement: 'top'}}>
  {Template.bind({})}
</Story>

<ArgsTable story="Default" />

## **Titulo**
El titulo es el contenido del tooltip y puede especificarse con la propiedad `title` o el slot `title`.

<Canvas>
  <Story name="Title"  parameters={renderParameters({showPanel: false})}>
    {() => {
      return {
        template: `
          <k-tooltip content="Lorem ipsum dolor sit">
            <k-button class="mr-4">Propiedad</k-button>
          </k-tooltip>
          <k-tooltip>
            <k-button>Slot</k-button>
            <template #content>
              Lorem ipsum dolor sit
            </template>
          </k-tooltip>
        `,
      };
    }}
  </Story>
</Canvas>

## **Posicion**
Puedes forzar la posición del tooltip con la propiedad `placement`

<Canvas>
  <Story name="Placement"  parameters={renderParameters({showPanel: false})}>
    {() => {
      return {
        template: `
          <k-tooltip placement="top" content="Tooltip on top">
            <k-button class="mr-2">TOP</k-button>
          </k-tooltip>
          <k-tooltip placement="left" content="Tooltip on left">
            <k-button class="mr-2">LEFT</k-button>
          </k-tooltip>
          <k-tooltip placement="bottom" content="Tooltip on bottom">
            <k-button class="mr-2">BOTTOM</k-button>
          </k-tooltip>
          <k-tooltip placement="right" content="Tooltip on right">
            <k-button>RIGHT</k-button>
          </k-tooltip>
        `,
      };
    }}
  </Story>
</Canvas>

## **Click/Hover**
Es posible modificar el compartamiento del tooltip para que se habra la propiedad `trigger`

<Canvas>
  <Story name="Method"  parameters={renderParameters({showPanel: false})}>
    {() => {
      return {
        setup() {
          const visible = ref(false);
          return {
            visible,
          };
        },
        template: `
          <k-tooltip trigger="mouseenter" content="Tooltip on hover">
            <k-button class="mr-2">HOVER</k-button>
          </k-tooltip>
          <k-tooltip trigger="click" hide-on-click content="Tooltip on click">
            <k-button class="mr-2">CLICK</k-button>
          </k-tooltip>
        `,
      };
    }}
  </Story>
</Canvas>

## **Flecha**
Puedes esconder la flecha del tooltip especificando la propiedad `arrow` a `false`

<Canvas>
  <Story name="Arrow"  parameters={renderParameters({showPanel: false})}>
    {() => {
      return {
        setup() {
          const visible = ref(false);
          return {
            visible,
          };
        },
        template: `
          <k-tooltip :arrow="false" content="Tooltip witouth arrow">
            <k-button>ARROW HIDE</k-button>
          </k-tooltip>
        `,
      };
    }}
  </Story>
</Canvas>
