import { Meta, Story, Canvas, Stories, ArgsTable } from '@storybook/addon-docs';
import KAccordion from './k-accordion.vue';
import { renderArgs, renderParameters } from '../../../helpers/storybook'
import SHead from '../../../../.storybook/app/components/s-head.jsx'

export const Template = (args) => ({
    components: { KAccordion },
    setup() {
      return { args };
    },
    template: `
    <k-accordion classes="p-4 mb-4 hover:bg-gray-500 hover:text-white rounded-md" active-classes="bg-success" inactive-classes="bg-gray-100">
      <k-accordion-item>
        <template #header>
          Item #1
        </template>
        <template #content>
          Content #1
        </template>
      </k-accordion-item>
      <k-accordion-item>
        <template #header>
          Item #2
        </template>
        <template #content>
          Content #2
        </template>
      </k-accordion-item>
      <k-accordion-item>
        <template #header>
          Item #3
        </template>
        <template #content>
          Content #3
        </template>
      </k-accordion-item>
    </k-accordion>`
});

<Meta title="Data Display/Accordion" component={KAccordion} argTypes={renderArgs(KAccordion)} decorators={[() => ({ template: '<div class="flex items-center justify-center p-10"><story /></div>' })]} />

<SHead image="https://media3.giphy.com/media/Cu87j5pPIL1hS/giphy.gif?cid=ecf05e47wwz771td1t86kiibuastopo4ywkqzs9oa71hvio8&rid=giphy.gif&ct=g">🤓 KAccordion</SHead>

# KButton
Con el componente `k-accordion` puedes definir un conjunto de cajas que se muestren o escondan a voluntad.

<Story name="Default" parameters={renderParameters({showPanel: true})}>
  {Template.bind({})}
</Story>

<ArgsTable story="Default" />
