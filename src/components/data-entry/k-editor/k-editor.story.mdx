import { Meta, Story, Canvas, Stories, ArgsTable } from '@storybook/addon-docs';
import KEditor from './k-editor.vue';
import { renderArgs, renderParameters } from '../../../helpers/storybook'
import SHead from '../../../../.storybook/app/components/s-head.jsx'
import { ref } from 'vue'

export const Template = (args) => ({
    components: { KEditor },
    setup() {
      return { args };
    },
    template: '<k-card><k-editor v-bind="args" /></k-card>'
});

<Meta title="Data Entry/Editor" component={KEditor} argTypes={renderArgs(KEditor)} decorators={[() => ({ template: '<div class="flex items-center justify-center p-10"><story /></div>' })]} />

<SHead image="https://media2.giphy.com/media/SwImQhtiNA7io/giphy.gif?cid=ecf05e47z82yq5bzlbtacqrphv0r4tr1wtbmnhzeakchqa9a&rid=giphy.gif&ct=g">⌨ KEditor</SHead>

# KEditor
Con el componente `k-editor` disponer de un editor de texto/html minimalista y bastante completo.

<Story name="Default" parameters={renderParameters({showPanel: true})} args={{placeholder:'Placeholder'}}>
  {Template.bind({})}
</Story>

<ArgsTable story="Default" />

## **Contenido Obligatorio**
Puedes definir un contenido obligatorio en el footer del editor con la propiedad `mandatory`

<Canvas>
  <Story name="MandatoryHTML" argTypes={renderArgs(KEditor, {}, true)} parameters={renderParameters({showPanel: false})}>
    {() => {
      return {
        components: { KEditor },
   
        template: `
          <k-card>
            <k-editor mandatory="Este campo es obligatorio" />
          </k-card>
        `,
      };
    }}
  </Story>
</Canvas>