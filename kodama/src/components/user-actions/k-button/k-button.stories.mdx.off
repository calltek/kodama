import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { ArgsTable } from '@storybook/addon-docs/blocks';
import KButton from './k-button.vue';


<Meta title="User Actions/Button" component={KButton} />

# Button with vue-class-component style syntax

With `MDX` we can define a story for `Hello World` right in the middle of our
Markdown documentation.



export const Template = (args) => ({
  components: { KButton },
  setup() {
    return { args };
  },
  template: '<k-button v-bind="args">Button</k-button>'
});


<Canvas>
    <Story name="Default">
        {Template.bind({})}
    </Story>
        
    <Story name="Secondary" args={{color: 'success',label: 'Positive'}}>
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="^" />